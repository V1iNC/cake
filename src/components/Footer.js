import React from 'react';

const Footer = () => (
  <footer className="footer">
    <div className="container">
      <p>© {new Date().getFullYear()} Сладкий Чек — Все права защищены</p>
    </div>
  </footer>
);

export default Footer;
